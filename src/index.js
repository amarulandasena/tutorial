const fn = () => {
    const miString = "!Soy un String muy feliz"
    let b = miString + "en esta vida."
    console.log(b)
}

fn()

const clickeado = () => {
    let a = "Amo la programción."

    console.log(a)
}